version: '3.8'

services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html
      - ./users.json:/var/www/html/users.json
      - ./error.log:/var/www/html/error.log
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    restart: unless-stopped
